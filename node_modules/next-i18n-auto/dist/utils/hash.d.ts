/**
 * Generate a unique ID for a text node based on component name and text content
 */
export declare function generateTextId(componentName: string, text: string, index?: number): string;
/**
 * Normalize text content by removing extra whitespace
 */
export declare function normalizeText(text: string): string;
/**
 * Check if text is translatable (not empty, not just whitespace or special chars)
 */
export declare function isTranslatableText(text: string): boolean;
