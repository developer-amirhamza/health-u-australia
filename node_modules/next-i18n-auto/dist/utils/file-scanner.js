"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.scanProjectFiles = scanProjectFiles;
exports.readFile = readFile;
exports.writeFile = writeFile;
exports.fileExists = fileExists;
const glob_1 = require("glob");
const path_1 = __importDefault(require("path"));
const promises_1 = __importDefault(require("fs/promises"));
/**
 * Scan project for JSX/TSX files
 */
async function scanProjectFiles(options) {
    const { rootDir, includeDirs = ['app', 'components', 'pages', 'src'], excludeDirs = ['node_modules', '.next', 'dist', 'build'], extensions = ['tsx', 'jsx'] } = options;
    const patterns = includeDirs.map(dir => `${dir}/**/*.{${extensions.join(',')}}`);
    const files = [];
    for (const pattern of patterns) {
        try {
            const matches = await (0, glob_1.glob)(pattern, {
                cwd: rootDir,
                absolute: true,
                ignore: excludeDirs.map(dir => `**/${dir}/**`)
            });
            files.push(...matches);
        }
        catch (error) {
            // Directory might not exist, skip it
            console.log(`Skipping pattern ${pattern}: directory not found`);
        }
    }
    return [...new Set(files)]; // Remove duplicates
}
/**
 * Read file content
 */
async function readFile(filePath) {
    return await promises_1.default.readFile(filePath, 'utf-8');
}
/**
 * Write file content
 */
async function writeFile(filePath, content) {
    await promises_1.default.mkdir(path_1.default.dirname(filePath), { recursive: true });
    await promises_1.default.writeFile(filePath, content, 'utf-8');
}
/**
 * Check if file exists
 */
async function fileExists(filePath) {
    try {
        await promises_1.default.access(filePath);
        return true;
    }
    catch {
        return false;
    }
}
