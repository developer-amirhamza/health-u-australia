"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateTextId = generateTextId;
exports.normalizeText = normalizeText;
exports.isTranslatableText = isTranslatableText;
const crypto_1 = __importDefault(require("crypto"));
/**
 * Generate a unique ID for a text node based on component name and text content
 */
function generateTextId(componentName, text, index = 0) {
    const cleanText = text.trim().replace(/\s+/g, ' ');
    const hash = crypto_1.default
        .createHash('md5')
        .update(cleanText)
        .digest('hex')
        .substring(0, 8);
    const cleanComponentName = componentName
        .replace(/[^a-zA-Z0-9]/g, '_')
        .toLowerCase();
    return `${cleanComponentName}_${hash}${index > 0 ? `_${index}` : ''}`;
}
/**
 * Normalize text content by removing extra whitespace
 */
function normalizeText(text) {
    return text.trim().replace(/\s+/g, ' ');
}
/**
 * Check if text is translatable (not empty, not just whitespace or special chars)
 */
function isTranslatableText(text) {
    const normalized = normalizeText(text);
    if (!normalized || normalized.length === 0)
        return false;
    // Skip if it's just numbers, symbols, or very short
    if (normalized.length < 2)
        return false;
    if (/^[\d\s\p{P}]+$/u.test(normalized))
        return false;
    return true;
}
