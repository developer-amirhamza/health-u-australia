"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Translator = void 0;
const node_fetch_1 = __importDefault(require("node-fetch"));
/**
 * Translate text using LibreTranslate API
 */
class Translator {
    constructor(options) {
        this.apiKey = options.apiKey;
        this.apiUrl = options.apiUrl || 'https://libretranslate.com/translate';
        this.sourceLanguage = options.sourceLanguage || 'en';
    }
    /**
     * Translate a single text to target language
     */
    async translate(text, targetLanguage) {
        try {
            const response = await (0, node_fetch_1.default)(this.apiUrl, {
                method: 'POST',
                body: JSON.stringify({
                    q: text,
                    source: this.sourceLanguage,
                    target: targetLanguage,
                    format: 'text',
                    api_key: this.apiKey
                }),
                headers: { 'Content-Type': 'application/json' }
            });
            if (!response.ok) {
                throw new Error(`Translation API error: ${response.statusText}`);
            }
            const data = await response.json();
            return data.translatedText;
        }
        catch (error) {
            console.error(`Error translating to ${targetLanguage}:`, error);
            throw error;
        }
    }
    /**
     * Translate multiple texts in batch
     */
    async translateBatch(texts, targetLanguage, onProgress) {
        const entries = Object.entries(texts);
        const total = entries.length;
        const results = {};
        for (let i = 0; i < total; i++) {
            const [key, text] = entries[i];
            try {
                results[key] = await this.translate(text, targetLanguage);
                if (onProgress) {
                    onProgress(i + 1, total);
                }
                // Add small delay to avoid rate limiting
                await new Promise(resolve => setTimeout(resolve, 100));
            }
            catch (error) {
                console.error(`Failed to translate key "${key}":`, error);
                // Fallback to original text if translation fails
                results[key] = text;
            }
        }
        return results;
    }
}
exports.Translator = Translator;
