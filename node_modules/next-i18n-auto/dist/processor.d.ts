export interface ProcessorOptions {
    rootDir: string;
    languages: string[];
    sourceLanguage?: string;
    outputDir?: string;
    apiUrl?: string;
}
export interface ProcessorResult {
    totalFiles: number;
    totalTexts: number;
    languages: string[];
    outputDir: string;
}
/**
 * Main processor for extracting and translating texts
 */
export declare class I18nProcessor {
    private options;
    private apiKey;
    constructor(options: ProcessorOptions);
    /**
     * Process all files and generate translations
     */
    process(): Promise<ProcessorResult>;
}
