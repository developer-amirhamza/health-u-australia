import type { Metadata } from "next";
// import { seoMetadata } from "@/config/seoMetadata";

// export const metadata: Metadata = seoMetadata.signup;

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}